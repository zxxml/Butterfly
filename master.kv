#:kivy 1.10.1

<MasterWidget>
    canvas:
        Color:
            rgb: (0, 0, 0, 1)
        Rectangle:
            size: self.size
            pos: self.pos
    Image:
        id: video
        size: self.parent.size
        on_size: self.size = self.parent.size
        allow_stretch: True
